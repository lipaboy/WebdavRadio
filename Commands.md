1. Initialize local venv environment
```powershell
py -m venv .venv
```

2. Activate local venv environment mode
```powershell
./.venv/Scripts/activate
```

3. Install package
```
pip install package --proxy http://username:pass@host:port
```

4. Install ffmpeg
```
winget install ffmpeg
pip install audioop-lts
```
